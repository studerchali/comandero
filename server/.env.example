# Puerto del servidor (default: 3001)
PORT=3001

# Host de escucha (default: 0.0.0.0)
# HOST=0.0.0.0

# Clave opcional: si se define, exige cabecera X-API-Key en todas las rutas excepto /api/health
# COMANDERO_API_KEY=